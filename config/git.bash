#!/usr/bin/env bash
# do not use `export` keyword in this file:
# shellcheck disable=SC2034

# Used by `setup-git`
# You can use `setup-git --configure` to configure these.

GIT_DEFAULT_BRANCH='main'
GIT_EMAIL='jon@spotsource.com'
GIT_NAME='Jon Penton'
GIT_PROTOCOL='ssh'
GITHUB_USERNAME='jon-spotsource'
# GITLAB_USERNAME=''
# GPG_SIGNING_AGENT=''

# 'op' for 1password, 'krypton' for Krypt.co, 'default' for system default
# GPG_SIGNING_KEY=''
MERGE_TOOL='delta'

# 'op' for 1password, 'krypton' for Krypt.co, 'default' for system default
# SSH_IDENTITY_AGENT=''
