#!/usr/bin/env bash
# do not use `export` keyword in this file:
# shellcheck disable=SC2034

# Used by `setup-git`
# You can use `setup-git --configure` to configure these.

# GPG_SIGNING_AGENT=''
# GPG_SIGNING_KEY='' # 'op' for 1password, 'krypton' for Krypt.co, 'default' for system default
# SSH_IDENTITY_AGENT='' # 'op' for 1password, 'krypton' for Krypt.co, 'default' for system default

GIT_DEFAULT_BRANCH='main'
GIT_EMAIL='jon@spotsource.com'
GIT_NAME='Jon Penton'
GIT_PROTOCOL='ssh'
GITHUB_USERNAME='jon-spotsource'
# GITLAB_USERNAME=''
MERGE_TOOL='delta'
